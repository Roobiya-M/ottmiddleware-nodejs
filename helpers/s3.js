'use strict';

const {
    updateCredentials,
    updateRegion,
    getAllBuckets,
    getUploadUrl,
    uploadFile,
    listFiles,
    deleteFiles,
} = require('s3-bucket');

module.exports = {
    createLog: (filePath, message) => {

    }
}